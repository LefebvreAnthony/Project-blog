{% for article in articles %}
    {% include 'article/item.html.twig' with { articles } %}
{% endfor %}

<div>
    <div>
        <div>
            {{ knp_pagination_render(articles) }}
        </div>
    </div>
</div>